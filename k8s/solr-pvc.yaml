apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solr-pvc
  namespace: vusta-idist-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gold-storage-service

